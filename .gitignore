# Secrets — never commit
.env

# Local reference material (Airalo screens from Mobbin — copyrighted, reference only)
mobbin-reference/

# OS noise
Thumbs.db
desktop.ini

# Build output & deps (site is built on deploy)
site/node_modules/
site/dist/
